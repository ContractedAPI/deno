# Task: component-types

## Overview

Implement the OpenAPI Components and Reference type definitions.

## Deliverables

- `ReferenceObject` type (`$ref` wrapper)
- `ComponentsObject` type (schemas, responses, parameters, etc.)
- `HttpMethod` type union

## File Target

`src/spec/openapi.ts` (extend existing)

## Estimated Scope

~30-40 lines of type definitions
