# Task: request-response-types

## Overview

Implement the OpenAPI Parameter, RequestBody, Response, and related types.

## Deliverables

- `ParameterObject` type (path, query, header, cookie parameters)
- `RequestBodyObject` type
- `ResponseObject` type
- `MediaTypeObject` type
- `HeaderObject` type
- `SecuritySchemeObject` and `SecurityRequirementObject` types

## File Target

`src/spec/openapi.ts` (extend existing)

## Estimated Scope

~60-80 lines of type definitions
