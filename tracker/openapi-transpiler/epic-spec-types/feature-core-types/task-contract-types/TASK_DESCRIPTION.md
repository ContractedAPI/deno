# Task: contract-types

## Overview

Implement the Contract and ContractCard types that define API contracts.

## Deliverables

- `Contract` type (minimal contract definition)
- `ContractCard` type (external contract file with defaults)
- `ContractCardPath` type and `isContractCardPath` type guard

## File Target

`src/spec/types.ts` (extend existing)

## Estimated Scope

~40-50 lines
