# Task: combined-emitter

## Overview
Create the main emit function that orchestrates all emission.

## Deliverables
- `emit()` main function
- `emitImports()` function
- `emitBaseTypes()` function

## File Target
`src/transpiler/typescript-emitter.ts`
