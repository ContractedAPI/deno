# Task: contracts-array

## Overview
Emit the contracts array with `as const` assertion.

## Deliverables
- `emitContractsArray()` function
- `emitContractObject()` function

## File Target
`src/transpiler/typescript-emitter.ts`
