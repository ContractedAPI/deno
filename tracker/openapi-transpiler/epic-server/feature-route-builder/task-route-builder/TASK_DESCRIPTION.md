# Task: route-builder

## Overview
RouteBuilder class for organizing routes in a tree.

## Deliverables
- `RouteBuilder` class
- `add()` method
- `match()` method

## File Target
`src/server/router.ts`
