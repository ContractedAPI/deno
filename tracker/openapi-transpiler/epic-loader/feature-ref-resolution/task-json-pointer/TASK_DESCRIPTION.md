# Task: json-pointer

## Overview

Implement JSON Pointer parsing for $ref resolution.

## Deliverables

- `parseJsonPointer(ref: string): string[]`
- `isLocalRef(ref: string): boolean`

## File Target

`src/loader/refs.ts`
