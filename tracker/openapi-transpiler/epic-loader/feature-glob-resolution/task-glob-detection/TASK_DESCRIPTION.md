# Task: glob-detection

## Overview

Implement detection of glob patterns in spec keys.

## Deliverables

- `isGlobPattern(key: string): boolean` function
- `GlobMatch` type

## File Target

`src/loader/glob.ts`
