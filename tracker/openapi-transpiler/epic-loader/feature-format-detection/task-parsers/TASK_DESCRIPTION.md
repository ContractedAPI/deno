# Task: parsers

## Overview

Implement parser wrapper functions for YAML, TOML, and JSON.

## Deliverables

- `parseYaml(content: string): unknown`
- `parseToml(content: string): unknown`
- `parseJson(content: string): unknown`
- `parse(content: string, format: Format): unknown`

## File Target

`src/loader/format.ts` (extend)

## Estimated Scope

~40-50 lines
